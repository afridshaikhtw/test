format_version: 3
pipelines:
  experiment:
    group: experiment
    display_order: -1
    label_template: "${COUNT}"
    lock_behavior: none
    materials:
      experiment:
        git: git@github.com:afridshaikhtw/test.git
        branch: main
        username: afridshaikhtw
    stages:
      - build_experiment:
          fetch_materials: true
          jobs:
            say_hello:
              timeout: 0
              tasks:
                - exec:
                    arguments:
                      - "Experimental pipeline"
                    command: echo
